apiVersion: v1
kind: Pod
metadata:
  name: node-selector
  labels:
    purpose: "selector-demo"
spec:
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1b  # we dont have worker nodes in this zone, so the pod will not be scheduled
  containers:
  - name: nginx
    image: nginx